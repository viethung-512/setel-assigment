import React from 'react';

import CreateOrderModal from '../../../features/order/CreateOrderModal';

const ModalManager: React.FC = props => {
  return (
    <span>
      <CreateOrderModal />
    </span>
  );
};

export default ModalManager;
